import 'package:flutter/material.dart';

class GenericUserCard extends StatelessWidget {
  const GenericUserCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
